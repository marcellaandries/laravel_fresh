composer create-project laravel/laravel laravel_fresh

php artisan serve -> to npm run serve

php artisan -> too see cmd
composer update -> to npm i

to clear cache:
php artisan cache:clear
php artisan view:clear
php artisan route:clear
php artisan clear-compiled
php artisan config:cache

***************************************************************************************
ROUTE
//url, page
Route::view('/about/cella', 'about_page');

============================================
//url view -> web route
//view
<ul>
    <li><a href="{{ url('/') }}">Home</li>
    <li><a href="{{ url('/about') }}">About</li>
</ul>   

//web route
Route::get('/', function () {
    return view('home');
})->name('front_home');

Route::get('/about', function () {
    return view('about_page');
})->name('front_about');

----------------------
// route formula:
// route path
// function to return view
// route name

// Route::get('/', function () {
//     return view('home');
// })->name('front_home');

// Route::get('/about', function () {
//     return view('about_page');
// })->name('front_about');

***************************************************************************************
CONTROLLER
php artisan make:controller StudentController

// controller formula:
// route path
// controller, function
// route name

https://laravel.com/docs/10.x/blade#displaying-unescaped-data

name in web should be the same with name in home.blade e.g student_home

